String - обьект, последовательность символов

Способы создания строк:
1. Строковый литерал
Проверяет - есть ли строка в пуле, возрвращает - если есть, добавляет - если нет
String s1 = "Hello";
2. Оператор new
Даже если есть обьект в пуле, создается новый обьект в куче
(Вообще литерал добавляется в пул - а потом копируется и создается обьект в куче)
String s2 = new String("Hello");
3. Конструкторы
Создает строки из массива символов или байт с помощью встроенных в класс String конструкторов(только через new)
char[] chars = {'H', 'e', 'l', 'l', 'o'};
String s3 = new String(chars);
еще можно байт-массив

Строки в java неизменяемы
Поэтому любая "изменяющая" операция - создает новый обьект String
Нужно для:
- Безопасности
- Кэширования и производительности
- Многопоточности

Литеральные и интернированные строки (и константы) хранятся в пуле, который в свою очередь находится в куче
Строки созданные new или динамически (считывание файла) - обычне обьекты в куче
intern() - проверяют есть ли строка в пуле, возрвращает - если есть, добавляет - если нет
Интернирование нужно как раз для обьектов в куче

Сравнение строк:
1. equals() - сравнивает содержимое строк
2. == - сравнивают ссылки на обьекты

Конкатенация строк:
1. plus
фактически вызывает СтрингБилдер под капотом
поэтому в цикле лучше явно использовать СтрингБилдер, чтобы не создавать новый обьект в каждой итераци, инче в цикле вылезет квадратичная сложность
Опять же - если выполняется на этапе компиляции (литералы и константы), то попадает в пул, иначе создается новый обьект в куче
2. concat()
Создает новый обьект в куче
не нуль безопасен
?немного быстрее "+"?
3. String.join()
Нуль безопасен, лаконичен, работает с любыми итерируемыми обьектами
Но всегда создает новую строку, даже если передан один элемент
4. StringJoiner()
Есть префикс и суффикс, использует Стринг Билдер
Полезен для JSON-массивов и Stream API потоков

- Просто склеить 2 строки → +
- Собрать строку в цикле → StringBuilder или StringJoiner
- Объединить коллекцию/массив с разделителем → String.join() или стримы
- Нужны префикс/суффикс → StringJoiner или Collectors.joining(..., ..., ...)

Основные методы класса String:
1. Длина строки
length()
2. Доступ к символам
charAt(index)
Выбрасывает исключение, если индекс вне диапазона
Для работы с сурогатными парами за диапазоном - codePointAt()
3. Поиск подстроки\символа
indexOf() //Первое вхожденние символа
lastIndexOf() //Первое вхождение подстроки
contains() //Синтаксический сахар, внутри вызывает indexOf
startsWith() //Проверяет префикс
endsWith() //Проверяет суффикс
4. Извлечение подстроки
substring(int beginIndex, int endIndex) 
Можно указать только первый индекс
5. Изменение регистра
toLowerCase()
toUpperCase()
6. Удаление пробелов в начале и конце строки
trim() 
strip()
stripLeading() //Удаляет только начальный
stripTrailing() //Удаляет только замыкающий
trim() удаляет только символы с кодом <= \u0020 (пробел, табуляция, перевод строки и т.д.), 
а strip() удаляет любые пробельные символы по Юникоду.
В новых версиях предпочтительнее - strip()
В старых версиях и когда нужна производительность - trim()
7. Замена
replace(char (CharSequence) oldChar, char newChar)
replaceAll() - С использованием регулярного выражения
replaceFirst() - заменяет толькое первое совпадение
8. Разделение строк
split(String regex, int limit) - (вторым аргументом можно задать ограничение)
Если строка начинается с разделителя - то первый элемент массива будет пустой элемент
Точку нужно экранировать \\.
!Аргумент в виде регулярного выржаения, но для запятой или пробела все просто
9. Проверка пустой строки
isEmpty() //Строка считается пустой, если length() == 0;
isBlank() //Строка считается пустой, если в ней нет символов кроме пробельных



StringBuilder и StringBuffer - классы изменяемой последовательности символов, включающие методы для работы с ними
Внутри представляют из себя char[] или byte[]
поле count - длина, используется в length()
если данные не влазят во внутренний массив - он автоматически расширяется (зачастую в два раза)
нужно использовать в циклах для быстродействия
удобен для форматирования вручную (для переопределения toString в моделях, например)

Для однопоточного кода - StringBulder
Для нескольких потоков - StringBuffer (потокобезопасен, но медленнее)

Почти все методы этих двух классов возвращают this, поэтому можно строить цепочки вызовов

Создание:
1) StringBuilder sb1 = new StringBuilder();
начальная ёмкость 16 символов
2) StringBuilder sb2 = new StringBuilder(100);
заданная ёмкость
3) StringBuilder sb3 = new StringBuilder("Hello");
с начальным содержимым

Методы:
1) Добавление
append() - перегружен для примитивов и обьектов
можно цепочкой
2) Вставка в указанную позицию
insert(позиция, аргумент)
3) Удаление
delete(с какой позиции, по какую(невключительно)) - удаляет подпоследовательность символов
deleteCharAt(позиция) - удаляет символ на позиции
4) Замена
replace(с какой позиции, по какую(невключительно), на что заменить) - заменяет подпоследовательность
5) Реверс
reverse() 
6) Получение строки
toString() - возвращает обычный неизменяемый String
можно использовать цепочку toString().toCharArray() - чтобы перейти к char[]
7) Управление емкостью
capacity() - текущая емкость внутреннего буфера
ensureCapacity(240) - гарантировать что емкость не меньше данной (например 240 как здесь)
trimToSize - уменьшить емкость до текущей длины (экономия памяти)
8) Длина и доступ к символам
length() - текущая длина содержимого
charAt() - получить символ по индексу
setCharAt() - установить символ по индексу
9) setLength() - позволяет урезать длину или увеличить, увеличение заполняется null - использовать с осторожностью!

!Сравнение через equals - не работает, нужно сначала преобразовать в строки!
Для эффективной вставки в начало - лучше сначала реверснуть

























